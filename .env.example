# ============================================================
# ENVIRONMENT VARIABLES - AUConnect
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit the actual .env file to version control.
# ============================================================

# Supabase (Required)
# The publishable/anon key is safe for client-side use.
# It is NOT a secret — it only works with RLS enabled.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_key_here

# Gemini API Key (Optional - for mess menu OCR feature)
# ⚠️ WARNING: Any key prefixed with VITE_ is exposed to the client bundle.
# This is acceptable ONLY if the key has strict usage quotas, IP restrictions,
# and limited permissions. For production, proxy this through a Supabase Edge
# Function to keep the key server-side.
VITE_GEMINI_API_KEY=your_gemini_key_here

# Cloudinary (Optional - for file uploads)
# These are public identifiers for unsigned uploads, NOT secrets.
# The upload preset must be configured as "unsigned" in the Cloudinary dashboard
# with strict file type and size restrictions enabled.
# If omitted, fallback values in storage.ts will be used.
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_upload_preset
